# Final SQL Project — Pandemic Database

## Опис проєкту
Цей репозиторій містить фінальний проєкт з реляційних баз даних.  
Метою проєкту є імпорт, нормалізація та аналіз даних про інфекційні захворювання з використанням MySQL.

База даних побудована на основі початкової таблиці `infectious_cases` та приведена до нормалізованого вигляду.

---

## 🗂 Структура бази даних

### 1. `infectious_cases`
Початкова таблиця з денормалізованими даними:
- entity
- code
- year
- показники захворювань (rabies, malaria, hiv тощо)

### 2. `entities`
Довідкова таблиця країн / регіонів:
- `id` — первинний ключ
- `entity` — назва країни / регіону
- `code` — код країни

Таблиця наповнена унікальними значеннями з `infectious_cases`.

### 3. `infectious_cases_norm`
Нормалізована таблиця:
- `entity_id` — зовнішній ключ → `entities(id)`
- `year`
- показники захворювань

---

## Виконані завдання

### ✅ Завдання 1
- Створено схему `pandemic`
- Імпортовано таблицю `infectious_cases`

### ✅ Завдання 2
- Створено таблицю `entities`
- Наповнено унікальними значеннями `entity` та `code`

### ✅ Завдання 3
- Створено нормалізовану таблицю `infectious_cases_norm`
- Дані перенесено з `infectious_cases` з використанням `JOIN`

### ✅ Завдання 4
- Написано SQL-запит з агрегатними функціями (`AVG`, `MIN`, `MAX`, `SUM`)
- Отримано **ТОП-10 країн за середнім значенням захворюваності на сказ (rabies)**

### ✅ Завдання 5
- Створено користувацьку SQL-функцію:
  ```sql
  year_diff_from_now(year)
Функція обчислює кількість років від початку заданого року до поточної дати
Функцію використано в SELECT запиті

Приклад використання функції
sql
Копировать код
SELECT
    year,
    MAKEDATE(year, 1) AS year_start_date,
    CURDATE() AS today_date,
    year_diff_from_now(year) AS years_diff
FROM infectious_cases_norm
ORDER BY year
LIMIT 10;

У репозиторії присутні скріншоти виконання кожного етапу:
01_schema_pandemic_created.png
02_entities_table_created.png
03_entities_table_filled.png
04_infectious_cases_norm_created.png
05_infectious_cases_norm_filled.png
06_aggregations_top10.png
07_function_year_diff_created_and_used.png

Технології:
MySQL 8.0
MySQL Workbench
GitHub
